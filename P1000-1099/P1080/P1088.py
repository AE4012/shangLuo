"""
# P1088 [NOIP 2004 普及组] 火星人

## 题目描述

人类终于登上了火星的土地并且见到了神秘的火星人。人类和火星人都无法理解对方的语言，但是我们的科学家发明了一种用数字交流的方法。这种交流方法是这样的，首先，火星人把一个非常大的数字告诉人类科学家，科学家破解这个数字的含义后，再把一个很小的数字加到这个大数上面，把结果告诉火星人，作为人类的回答。

火星人用一种非常简单的方式来表示数字――掰手指。火星人只有一只手，但这只手上有成千上万的手指，这些手指排成一列，分别编号为 $1,2,3,\cdots$。火星人的任意两根手指都能随意交换位置，他们就是通过这方法计数的。

一个火星人用一个人类的手演示了如何用手指计数。如果把五根手指――拇指、食指、中指、无名指和小指分别编号为 $1,2,3,4$ 和 $5$，当它们按正常顺序排列时，形成了 $5$ 位数 $12345$，当你交换无名指和小指的位置时，会形成 $5$ 位数 $12354$，当你把五个手指的顺序完全颠倒时，会形成 $54321$，在所有能够形成的 $120$ 个 $5$ 位数中，$12345$ 最小，它表示 $1$；$12354$ 第二小，它表示 $2$；$54321$ 最大，它表示 $120$。下表展示了只有 $3$ 根手指时能够形成的 $6$ 个 $3$ 位数和它们代表的数字：


| 三位数 | 代表的数字 |
|:-:|:-:|
| $123$ | $1$ |
| $132$ | $2$ |
| $213$ | $3$ |
| $231$ | $4$ |
| $312$ | $5$ |
| $321$ | $6$ |

现在你有幸成为了第一个和火星人交流的地球人。一个火星人会让你看他的手指，科学家会告诉你要加上去的很小的数。你的任务是，把火星人用手指表示的数与科学家告诉你的数相加，并根据相加的结果改变火星人手指的排列顺序。输入数据保证这个结果不会超出火星人手指能表示的范围。

## 输入格式

共三行。  
第一行一个正整数 $N$，表示火星人手指的数目（$1 \le N \le 10000$）。  
第二行是一个正整数 $M$，表示要加上去的小整数（$1  \le  M  \le  100$）。  
下一行是 $1$ 到 $N$ 这 $N$ 个整数的一个排列，用空格隔开，表示火星人手指的排列顺序。

## 输出格式

$N$ 个整数，表示改变后的火星人手指的排列顺序。每两个相邻的数中间用一个空格分开，不能有多余的空格。

## 输入输出样例 #1

### 输入 #1

```
5
3
1 2 3 4 5

```

### 输出 #1

```
1 2 4 5 3
```

## 说明/提示

对于 $30\%$ 的数据，$N \le 15$。

对于 $60\%$ 的数据，$N \le 50$。

对于 $100\%$ 的数据，$N \le 10000$。

noip2004 普及组第 4 题
"""

def next_figure_list(s):
    """ 
    生成给定序列的下一个字典序排列（原地修改）
    
    参数:
        s (list): 输入的整数列表，表示当前排列
        
    返回:
        bool: 如果成功生成下一个排列返回True，否则返回False（表示已是最大排列）
    """
    # Step1: 从右往左找第一个s[i] < s[i+1]的位置i
    # 原理：i右边的部分是一个非递增序列，已经是该子序列的最大排列
    i = len(s) - 2  # 从倒数第二个元素开始比较
    while i >= 0 and s[i] >= s[i+1]:  # 找第一个下降点
        i -= 1
        if i < 0:  # 如果遍历完整个序列都没找到下降点，说明已是最大排列
            return False
    
    # Step2: 从右往左找第一个比s[i]大的位置j
    # 原理：要找到最小的可以与s[i]交换的元素，保证新排列是最小的可能的更大排列
    j = len(s) - 1  # 从最后一个元素开始找
    while j >= 0 and s[j] <= s[i]:  # 找第一个比s[i]大的元素
        j -= 1

    # Step3: 交换s[i]和s[j]
    # 原理：这样可以使序列在位置i处增大，且是最小程度的增大
    s[i], s[j] = s[j], s[i]

    # Step4: 将i右边的部分反转
    # 原理：交换后i右边的部分仍然是递减的，反转后变为递增，这样能得到最小的可能排列
    s[i+1:] = reversed(s[i+1:])  # 原地修改列表，反转i+1到末尾的部分
    return True  # 成功生成下一个排列


if __name__ == "__main__":
    N = int(input().strip())
    M = int(input().strip())
    # 火星人手指排序
    mars = list(map(int, input().strip().split()))

    for _ in range(M):
        next_figure_list(mars)

    print(' '.join(map(str, mars)))